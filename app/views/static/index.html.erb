
<h1>Bienvenue dans le site <strong>Cool Mada</strong> ⛵️🎓😇</h1>
<div class="shadow mt-5 p-5 bg-light">
  <h2>Liste des utilisateurs</h2>
  <table class="table table-striped table-dark">
    <thead>
      <tr class="bg-primary">
        <th scope="col">#</th>
        <th scope="col">Username</th>
        <th scope="col">Bio</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>

      <%=%>
    
      <% @users.each do |u| %>
      <tr>
        <th scope="row"><%= u.id%></th>
        <td><%= u.username%></td>
        <td><%= u.bio%></td>
        <td><a href="<%= get_user_path(u.id)%>" class="btn btn-sm btn-info">Voir</a></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>

